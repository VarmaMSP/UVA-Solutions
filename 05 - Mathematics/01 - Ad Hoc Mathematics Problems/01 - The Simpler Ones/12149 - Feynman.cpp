#include <bits/stdc++.h>


int main() {
    int n;
    while (scanf("%d", &n) && n) {
        printf("%d\n", (n * (n + 1) * (2 * n + 1)) / 6);
    }
    return 0;
}

/*
Author: bumpy (-_-)
date: 8-Aug-2018
*/

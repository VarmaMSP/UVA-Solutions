#include <bits/stdc++.h>

using namespace std;

int main() {
    int tt;
    scanf("%d", &tt);
    while (tt--) {
        scanf("%d", &m);
        for (int i = 0; i < m; ++i) {
            scanf("%d", a + i);
        }

    }
}
